name: Project 1 CI - Intelligent Document Fraud Agent

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  test-agent:
    name: Run Unit Tests & Lint
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Pixi Environment
        uses: prefix-dev/setup-pixi@v0.10.0
        with:
          # Point setup-pixi to your project's pixi.toml
          manifest-path: "project1/pixi.toml"
          cache: true

      - name: Run Test Suite
        # Tells GitHub Actions to run the command inside your project folder
        working-directory: ./project1
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY || 'sk-dummy-key-for-tests' }}
        run: |
          pixi run pytest -v tests/